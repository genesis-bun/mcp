# MCP Server State File
# Customize this structure to match your application's needs.
# After modifying, update the schema in schema.ts to match your structure.
# The schema provides type safety and runtime validation.

version: "1.0.0"
data:
  # Example structure - customize as needed
  # Update schema.ts to match your structure
  example_field: ""
